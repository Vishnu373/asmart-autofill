{
  "name": "@asmart/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.content.config.ts && bun check-content.mjs"
  },
  "dependencies": {
    "@asmart/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "vite": "^7.1.12"
  }
}
